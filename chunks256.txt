[{"AllowMultiple":false,"EndLine":182,"ReplacementContent":"  const canUseApprovals =\r\n    profileResult?.status === 'linked' && Number(profileResult.profile.authorityLevel ?? 1) > 1;\r\n  const isStoreManager =\n    profileResult?.status === 'linked' &&\n    (profileResult.profile.positionName ?? '').toLowerCase().includes('store manager');\n","StartLine":178,"TargetContent":"  const canUseApprovals =\r\n    profileResult?.status === 'linked' && Number(profileResult.profile.authorityLevel ?? 1) > 1;\r\n  const canManageTeam =\n    profileResult?.status === 'linked' && Number(profileResult.profile.authorityLevel ?? 1) > 1;\n"},{"AllowMultiple":false,"EndLine":282,"ReplacementContent":"  useEffect(() => {\r\n    if (activeTab === 'approvals' && !canUseApprovals) {\r\n      setActiveTab('perks');\r\n    }\r\n    if (activeTab === 'team' && !isStoreManager) {\n      setActiveTab('home');\n    }\n  }, [activeTab, canUseApprovals, isStoreManager]);\r\n","StartLine":274,"TargetContent":"  useEffect(() => {\r\n    if (activeTab === 'approvals' && !canUseApprovals) {\r\n      setActiveTab('perks');\r\n    }\r\n    if (activeTab === 'team' && !canManageTeam) {\n      setActiveTab('home');\n    }\n  }, [activeTab, canUseApprovals, canManageTeam]);\r\n"},{"AllowMultiple":false,"EndLine":723,"ReplacementContent":"    let tabContent: ReactNode = null;\r\n    const openTeam = isStoreManager ? () => setActiveTab('team') : undefined;\n    if (activeTab === 'requests') {\r\n","StartLine":703,"TargetContent":"    let tabContent: ReactNode = null;\r\n    if (activeTab === 'requests') {\r\n"},{"AllowMultiple":false,"EndLine":726,"ReplacementContent":"      tabContent = (\n        <RequestsTabScreen\n          profileResult={profileResult}\n          notificationCount={notificationUnreadCount}\n          onAssistant={openAssistant}\n          onNotifications={openNotifications}\n          onOpenTeam={openTeam}\n          showTeamLink={isStoreManager}\n        />\n      );","StartLine":705,"TargetContent":"      tabContent = (\n    
<truncated 4983 bytes>